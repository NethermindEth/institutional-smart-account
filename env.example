# Environment template for `implementation/`
#
# Cursor blocks committing dot-env templates like `.env.example` in this workspace,
# so this file is named `env.example`. Copy it to `.env` locally:
#
#   cp env.example .env
#
# IMPORTANT: never commit real secrets.

# -----------------------------------------------------------------------------
# Hardhat deployer key (required for public networks)
# -----------------------------------------------------------------------------
# Use a 0x-prefixed hex private key (Ethers/Hardhat compatible).
PRIVATE_KEY=

# -----------------------------------------------------------------------------
# Network RPC URLs (used by `implementation/hardhat.config.ts`)
# -----------------------------------------------------------------------------
SEPOLIA_RPC_URL=
BASE_SEPOLIA_RPC_URL=
MAINNET_RPC_URL=
BASE_RPC_URL=
ARBITRUM_RPC_URL=
OPTIMISM_RPC_URL=

# -----------------------------------------------------------------------------
# Explorer API keys (optional; used for verification)
# -----------------------------------------------------------------------------
ETHERSCAN_API_KEY=
BASESCAN_API_KEY=
ARBISCAN_API_KEY=
OPTIMISTIC_ETHERSCAN_API_KEY=

# -----------------------------------------------------------------------------
# Gas reporting (optional)
# -----------------------------------------------------------------------------
# Set REPORT_GAS to any value to enable gas reporter during tests.
REPORT_GAS=
COINMARKETCAP_API_KEY=

# -----------------------------------------------------------------------------
# Common script variables
# -----------------------------------------------------------------------------
# ERC-4337 EntryPoint Address (v0.7 recommended)
# Mainnet/Sepolia/Base: 0x0000000071727De22E5E9d8BAf0edAc6f37da032
ENTRYPOINT_ADDRESS=0x0000000071727De22E5E9d8BAf0edAc6f37da032

# Optional override signers for the example account created by `scripts/deploy.ts`
LEVEL1_SIGNER1=
LEVEL1_SIGNER2=
LEVEL1_SIGNER3=
LEVEL2_SIGNER1=
LEVEL2_SIGNER2=
LEVEL3_SIGNER1=

# Configure an existing account (used by `scripts/setup-config.ts`)
ACCOUNT_ADDRESS=

# Hardhat verification helper (used by `scripts/verify.ts`)
CONTRACT_ADDRESS=
# Comma-separated constructor args, e.g.:
# CONSTRUCTOR_ARGS=0xEntryPointAddress
CONSTRUCTOR_ARGS=

# -----------------------------------------------------------------------------
# Bundler / ERC-4337 integration tests
# -----------------------------------------------------------------------------
# Skandha bundler URL used by test harness
SKANDHA_BUNDLER_URL=http://localhost:14337/rpc

# Skip bundler-related tests/setup entirely
SKIP_BUNDLER=

# Keep the bundler running after tests finish
KEEP_BUNDLER_RUNNING=

# Extra logging from bundler setup
DEBUG_BUNDLER=
